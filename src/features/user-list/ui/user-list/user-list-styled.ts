'use client';
import styled from 'styled-components';

export const UserItemsList = styled.ul`
  display: flex;
  flex-direction: column;
  gap: 10px;
`;

export const UserListWrapper = styled.div`
  max-width: 500px;
  margin: 0 auto;
`;
